const request = require('supertest');
const app = require('../server');

describe('Testing video upload, list of videos request, video download', () => {
    it('uploads video in the folder', () => {
        // const file = fs.readFileSync('./tests/sample.mp4').toJSON();
        // console.log(file);
    });
});
